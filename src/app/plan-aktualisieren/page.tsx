import { PlanUploadFlow } from "@/components/plan/plan-upload-flow";

export const metadata = {
  title: "Plan aktualisieren · Coffee Morning",
};

export default function PlanUploadPage() {
  return <PlanUploadFlow />;
}
