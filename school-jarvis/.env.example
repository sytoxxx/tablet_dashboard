# School Jarvis — environment

# Shared secret with Coffee Morning (server-to-server only).
# Coffee Morning uses the same value as SCHOOL_JARVIS_API_TOKEN.
# Never expose this token to browsers.
COFFEE_MORNING_API_TOKEN=

# Which Coffee Morning person this integration serves (Phase 14: levi)
COFFEE_MORNING_PERSON_ID=levi
